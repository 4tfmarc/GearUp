'use client';
// ...existing code...

const handleAddToCart = (e) => {
  e.preventDefault(); // Add this to prevent any default browser behavior
  addToCart(product);
};

// ...existing code...
